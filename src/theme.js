// theme.js
import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    heading: `'Comforter', sans-serif`,
    body: `'Coustard', sans-serif`,
  },
})

export default theme